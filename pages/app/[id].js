// `/app/[id]` uses the same shell as `/app`; project detail (incl. “Other languages” + append
// translation tabs) is implemented in `Dashboard` in `pages/index.js`.
export { default } from "../authenticatedAppShell";
